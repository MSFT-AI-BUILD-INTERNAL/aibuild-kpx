# Round 18 — Plan

## Scope
- **F21 (실행)**: `CHANGELOG.md`의 미발표 항목들을 `[0.3.0] - 2026-05-25`로 릴리스 헤더 전환. 다음 사이클을 위한 빈 `[Unreleased]` 섹션 유지.
- **F5 (차단 유지)**: `gpt-4o-mini` × 37 케이스 표본 보강은 `GITHUB_TOKEN` 환경변수 미설정으로 라이브 API 호출 불가. 결과 합성 금지 원칙에 따라 별도 라운드로 분리.

## Rationale
- 사용자가 "1,2" 선택 → F5 + F21 동시 진행 의도. 그러나 사전 점검에서 토큰 부재 확인.
- F21은 결정론적이고 가역이며 R17의 [Unreleased] 갱신을 정식 릴리스로 굳히는 자연스러운 후속.
- F5를 가짜 데이터로 채우면 Orin "합의되지 않은 항목 비포함" 신조 위반 — 분할 실행이 정직한 선택.

## Acceptance
- `CHANGELOG.md` 최상단: 빈 `[Unreleased]` → `[0.3.0] - 2026-05-25` (R5–R16 누적 항목 포함).
- `pytest -q` 55 PASS 유지.
- F5는 R19 후보로 백로그 잔존(토큰 필요).
