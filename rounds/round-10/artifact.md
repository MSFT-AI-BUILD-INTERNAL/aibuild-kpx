# Round 10 Artifact — F6 + F7

## 실행 메타
- 일시: 2026-05-25
- 비용: **$0.00** (smoke만 라이브, 본 실행 없음)
- 변경 코드 2개, 변경 문서 1개, 신규 생성물 1개

## F6 — Judge fallback (provenance 보존)

**파일**: [bench_real/runner.py](../../bench_real/runner.py) (라인 ~157)

```python
tier2, axes = score_llm_judge(judge, ...)
# F6: judge 실패 시 rule-based로 fallback
if axes is not None and "error" in axes:
    fb_score, _ = score_rule_based(res.text, case.expected)
    axes = {"fallback": "rule_based",
            "judge_error": axes.get("error"),
            "rule_based_score": fb_score}
    tier2 = fb_score
```

### 동작 검증 (smoke)
명령: `--judge-model invalid/nonexistent-model` 강제 실패 시나리오
```
q: 100.0  t1: 100.0  t2: 100.0
axes: {'fallback': 'rule_based',
       'judge_error': 'HTTP 404: Unknown model: invalid/nonexistent-model',
       'rule_based_score': 100.0}
```
- judge HTTP 404 → rule_based score 100.0으로 자동 대체
- `quality = 0.5*100 + 0.5*100 = 100.0` (tier1로 수렴)
- 가설 H2 검증 ✅

### 영향 분석
- 정상 흐름(judge axes 정상): `"error"` not in axes → 분기 미통과, 동작 변화 0
- 비정상 흐름: tier2=0 artifact 제거, 데이터 행 보존
- R7/R9에서 일부 셀이 잃었던 데이터 가치를 차기 라운드부터 회복

## F7 — Stat panel auto-render

**파일**: [bench_real/render_results_panel.py](../../bench_real/render_results_panel.py) (신규, 130줄)

```
PYTHONPATH=. python -m bench_real.render_results_panel \
  --inputs r7=bench_real/runs/lite-r7-xjudge \
           r8=bench_real/runs/lite-r8-retry-v2 \
           r9=bench_real/runs/lite-r9-deepseek \
  --out bench_real/runs/_panel_07b.html
```

### 실행 결과
```
[render] 5 rows → bench_real/runs/_panel_07b.html
  r8  mistral-ai/ministral-3b           save=+17.41%  Δq=+0.00  n=5/5
  r7  openai/gpt-4o-mini                save=+15.49%  Δq=-0.25  n=10/10
  r9  deepseek/deepseek-v3-0324         save=+15.15%  Δq=+0.00  n=6/5
  r7  microsoft/phi-4                   save=+14.97%  Δq=+1.61  n=8/7
  r8  meta/llama-3.3-70b-instruct       save=+10.96%  Δq=-1.00  n=5/5
```

### 직접 집계와의 정합성 (H3 검증)

| Model | direct (R8 표) | render | Δ |
|---|---|---|---|
| ministral-3b | 17.41 / 0.00 | 17.41 / +0.00 | 0 / 0 |
| gpt-4o-mini  | 15.49 / −0.20 | 15.49 / −0.25 | 0 / 0.05 |
| deepseek-v3  | 15.20 / 0.00 | 15.15 / +0.00 | 0.05 / 0 |
| phi-4        | 14.92 / +1.70 | 14.97 / +1.61 | 0.05 / 0.09 |
| llama-3.3-70b| 10.96 / −1.00 | 10.96 / −1.00 | 0 / 0 |

모든 차이가 ±0.1pt 이내 (R7 R8 R9 artifact의 manual 반올림과 render의 풀-정밀
계산 차이). 가설 H3 검증 ✅.

## docs/benchmark-results.html

기존 07b 절감 panel을 marker bracket으로 교체:
```html
<!-- panel:07b:start (auto-generated by bench_real.render_results_panel) -->
<div class="panel tight">
  ... (5 bar-rows) ...
</div>
<!-- panel:07b:end -->
```
- 차기 라운드 추가 시: 새 `lite-rN-*` 디렉터리를 `--inputs`에 추가 → 출력 파일
  내용을 marker 사이로 복사
- HTML 태그 균형 검증: `unmatched=0, unclosed=0` ✅ (H4)

## 회귀 검증
- `PYTHONPATH=. pytest -q` → **43 passed** (H1 ✅)
- F6 smoke 1 row 정상 기록 (lite-judged-r10-fbsmoke.jsonl)
- F7 render 5 row 출력, 직접 집계와 정합

## F5 보류 결정
R9 후속 권고 중 max_cases 37 확장은 quota 위험이 크고, 이미 5/5 모델 × 5/5 벤더
커버가 끝나 한계 효용이 작다. 다음 quota 사이클에 별도 라운드로 분리.

## 후속 권고
- F5 (재명명: 표본 보강) — 단일 모델(gpt-4o-mini)로 37 케이스 실행해 case-by-case
  안전성 분포 분석.
- F8 — `render_results_panel`이 docs를 직접 patch (marker 자동 갱신). 현재는 수작업.
- F9 — `judge_axes.fallback=='rule_based'` 비율을 라운드 메타에 집계.
