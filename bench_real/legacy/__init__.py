"""Legacy v0.2.0 self-QA benchmark.

Preserved as the source of the **+21.36% all_safe token-saving** claim
referenced in CHANGELOG.md and docs/. New benchmark work lives in
``bench_real/`` (parent package). Run with::

    python -m bench_real.legacy.runner

Outputs ``bench_real/legacy/results.json``.
"""
